# Tools Folder

This folder keeps non-canonical helper scripts out of the repo root.

## Layout

- `legacy/`: older one-off scripts retained for reference only

## Active tooling

The active orchestration entry points are:

- `orchestra/scripts/Build-LoreAuditPackets.ps1`
- `.agent/skills/novel-orchestra-conductor/scripts/build_work_packet.py`
- `backend/*.py`

Anything in `tools/legacy/` should be treated as historical or ad hoc unless it is explicitly revalidated.
