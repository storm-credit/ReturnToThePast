# 설정집 1차 마감 감사 보고서

Date: 2026-04-07
Mode: `SETTING-FIRST`
Latest Smoke Audit: `PASS`
Smoke Report: `orchestra/runs/setting-smoke-20260407-160514/00_summary.md`

## 현재 판정

현재 설정집 판정은 `PASS`다.

이 판정은 "출고 완료"가 아니라, **설정집 선완성 모드 기준으로 집필 투입이 가능한 1차 마감 상태**라는 뜻이다.

즉 지금은:

- 시리즈 뼈대가 잠겨 있다.
- 시간여행 패러독스 프레임이 흔들리지 않는다.
- 권별 설계와 설정집의 충돌이 자동감사 기준에서 통제되고 있다.
- 이름, 용어, 톤, 복선, 세력, 장소 문서가 같은 결로 읽힌다.

## 잠긴 기준

- 총 15권
- 권당 25화
- 화당 공백 제외 3,500자 이상
- 작품 프레임은 `회귀물`이 아니라 `12 몽키즈형 시간여행 패러독스 다크 판타지`
- `outline`과 `timeline`은 설정 감사의 필수 입력
- 오케스트라는 설정집 먼저, 집필은 그다음
- 웹소설 문체 우선
- 중2병 대사 금지
- 중2병 서사 금지
- 운명론 허세, 선택받은 자 감성, 자기 신화화, 과장된 파멸 미학, 세계 상대로 한 장엄한 독백 금지

## 이번 1차 마감에서 확인된 강점

### 1. 구조

- 15권 전체의 `Outline/Timeline` 쌍이 존재한다.
- 주요 브리지 약점이었던 `제1권 -> 제2권`, `제6권 -> 제7권`, `제12권 리빌 과밀`이 이미 보강됐다.
- 고정점, 분기점, 패러독스 압력, 감정 대가가 권 단위 설계와 분리되지 않고 함께 읽힌다.

### 2. 캐논

- `강제 전송`이 진실이고 `회귀처럼 보이는 표면`이 외피라는 프레임이 잠겼다.
- `리아 구출`, `찰나`, `시간의 감옥`, `영시`, `빚과 징수` 축이 서로 다른 문서에서 같은 캐논으로 읽힌다.
- 엔딩 수렴 지도, 전반부 복선 지도, 측면 인물 목격 지도, 고정점 압력 지도가 서로 연결된다.

### 3. 표면

- 핵심 설정 문서의 `Vol.` 표기, 섞여 있던 영어 부제, 직선적인 메타 어휘를 대거 걷어냈다.
- `상아 의정회`, `녹투르`, `알바른`, `하층 권세도` 같은 정식 표면이 활성 문서 기준으로 잠겼다.
- 고풍 판타지 어감과 느와르 결이 설정집 표면에서 크게 벗어나지 않게 정리됐다.

### 4. 운영

- 오케스트라는 `설정집 전용`으로 다시 잠겼다.
- 자동감사 하네스가 `권 쌍`, `25화`, `캐논 충돌`, `복선 마커`, `엔딩 마커`, `증언 마커`를 빠르게 잡는다.
- 진입 문서와 인덱스가 새 기준 문서를 따라가도록 재정렬됐다.

## 이번 마감에서 반영된 큰 묶음

- 이벤트 카드 파일명을 제국력 기준으로 통일
- 코어 설정/가이드 문서 한국어 표면 동기화
- 하층 세력 문서와 권세도 경로 동기화
- 보조 로그와 보조 데이터의 노출 표면 정리
- 문체 가이드와 프롬프트 가이드의 웹소설 기준 정리
- 설정집 선완성 모드 문서화

## 남은 것

남은 것은 대부분 `필수 수정`이 아니라 `선택 정리`다.

### 선택 정리 A: 엔진용 JSON 키

- `lore_bible/characters/Kael_psych.json`
- `lore_bible/characters/Ria_psych.json`

여기 남은 `ideal` 같은 키는 현재 사람용 문서 표면이 아니라 엔진/구조용 스키마다.
즉 지금 당장 설정집 완성도를 해치지는 않는다.

### 선택 정리 B: 오케스트라 메타 문서 영어

오케스트라 내부 운영 문서 가운데 일부는 아직 영어 제목이나 영어 상태값을 유지한다.
다만 이것은 독자-facing 설정집 문제라기보다 운영자-facing 메타 문서 문제다.

### 선택 정리 C: 보조 파일 라인엔딩/로컬 산출물

- `.obsidian/*`
- `orchestra/runs/*`

이 둘은 로컬 작업 흔적이며, 설정집 마감 판정과는 분리한다.

## 총평

지금 설정집은 **"계속 보강 중인 미완 뼈대"** 단계는 지났다.
현재 위치는 **"집필 투입 가능한 1차 마감본"**에 가깝다.

다음부터는 큰 구조를 다시 뒤엎기보다:

1. 집필 직전 패킷을 만들고
2. 필요한 권/인물/세력만 좁게 재검수하고
3. 초안에서 생기는 미세 충돌을 다시 설정집으로 환류하는 방식

으로 가는 편이 훨씬 효율적이다.
